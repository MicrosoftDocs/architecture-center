<a href="./category/{{ category.url }}.md">

    <img src="_images/category/{{ category.url }}.svg" alt="{{ category.title }}" />

    <div class="cardText">
        <h3>{{ category.title }}</h3>
        <p>{{ category.description }}</p>
    </div>

</a>